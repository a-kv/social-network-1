const LOGIN = 'LOGIN';

let initialState ={

}

const LoginReduser = (state = initialState, action) =>{
    switch (action.type) {
        case LOGIN: {

        }
        default: {
            return state
        }
    }
}